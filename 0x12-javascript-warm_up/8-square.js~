#!/usr/bin/node
// This script prints a square
const myArgs = process.argv.slice(2);
if (myArgs.length === 0 || isNaN(myArgs[0])) {
  console.log('Missing size');
} else {
  for (let i = 0; i < myArgs[0]; i++) {
    for (let j = 0; j < myArgs[0]; j++) {
      console.log('X');
    }
  }
}
