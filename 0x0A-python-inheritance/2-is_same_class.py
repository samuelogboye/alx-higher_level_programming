#!/usr/bin/python3
"""This module returns is an object is an instance of a class"""


def is_same_class(obj, a_class):
    """a function returns True if its an instance of a class"""
    return (type(obj) == a_class)
